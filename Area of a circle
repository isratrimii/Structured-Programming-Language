#include <stdio.h>

int main()
{
    float radius, area;
    printf("Enter the radius of a circle:\n");
    scanf("%f", &radius);
    area = 3.1416 * radius * radius;
    printf("Area = %f\n", area);  

    return 0;
}
